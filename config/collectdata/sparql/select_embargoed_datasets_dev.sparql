PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX owl: <http://www.w3.org/2002/07/owl#>

# BDS CHANGED BEHAVIOR: deletes deprecated entities

SELECT DISTINCT ?s

WHERE {
?s owl:deprecated ?deprecated .
FILTER(?deprecated=true) .
FILTER(isIRI(?s))
}
